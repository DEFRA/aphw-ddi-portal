{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from "govuk/components/fieldset/macro.njk" import govukFieldset %}

{% extends '_layout.njk' %}

{% block pageTitle %}{{ serviceName }}{% endblock %}

{% block beforeContent %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-one-half">
      {{ govukBackLink({
        text: "Back",
        href: model.backLink,
        attributes: {
          id: "back"
        }
        }) 
      }}
    </div>
  </div>
{% endblock %}

{% block content %}
<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">

      <form class="form" method="post">

      <h1 class="govuk-heading-l">
        There’s an existing dog record with microchip number {{ model.microchipNumber }}
      </h1>

      {% for result in model.results %}
        {% include "./cdo/search/dog-results.njk" %}
      {% endfor %}

      <p class="govuk-body">
        Look at the original paperwork for:
      </p>

      <ul class="govuk-list govuk-list--bullet">
        <li>the dog you are adding to check you’ve entered the microchip number correctly</li>
        <li>the existing dog record that has the same microchip number to check if an error was made when it was first added to the index</li>
      </ul>

      <p class="govuk-body govuk-!-margin-bottom-0">
        If you find an error with the data, you should use the original paperwork to correct the data for the record which has the error.
      </p>
      <br/>
      <p class="govuk-body">
      If you do not find an error with the data, you should escalate to a line manager to agree the next step.
      </p>

      <p class="govuk-body">
        If you entered the number incorrectly, <a class="govuk-link govuk-link--no-visited-state" href="/cdo/create/microchip-search">change the microchip number</a>
      </p>

    </form>
  </div>
</div>
{% endblock %}
