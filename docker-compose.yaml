services:
  dangerous-dogs-act-portal:
    build:
      context: .
      target: production
    image: dangerous-dogs-act-portal
    container_name: dangerous-dogs-act-portal
    environment:
      APPINSIGHTS_CLOUDROLE: dangerous-dogs-act-portal-local
      APPINSIGHTS_CONNECTIONSTRING: ${APPINSIGHTS_CONNECTIONSTRING}
      OS_PLACES_API_KEY: ${DDA_OS_PLACES_API_KEY}
      COOKIE_PASSWORD: who-likes-cookies-i-like-cookies-everybody-likes-cookies
